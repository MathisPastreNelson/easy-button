{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build --base /easy-button/",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "nuxt generate && npx gh-pages -d dist"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "@types/node": "^18",
    "gh-pages": "^5.0.0",
    "nuxt": "^3.5.2"
  }
}
